# 技术选型指南 Skill

## 目的

在自动编码过程中，确保 AI 推荐的技术栈和框架选型在团队可接受的范围内。避免引入冷门、过时或不在维护周期内的框架。

## 使用方式

本 skill 作为 steering 规则自动生效，当 Kiro 在以下场景中工作时会参考此选型指南：

- 新建项目脚手架
- 推荐依赖或框架
- 编写技术方案
- 代码生成涉及框架选择时

## 维护

编辑 `frameworks.yaml` 来调整你的可接受框架范围。将不需要的框架 `status` 改为 `excluded` 即可排除。
